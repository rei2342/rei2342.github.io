# プロジェクトメモリ — 高単価アフィ運用（playbook）

> このファイルは Claude Code が `affiliate-research-engine/` で作業するとき自動で読み込む永続メモリ。
> 第7章テク29（Memory）の「毎回前提を貼り直さない」を、まずこのファイルだけで実現する。
> **値が決まったら更新する。`要確定` は次のセッションで埋める。**

## 運用システム
- 設計図と実装手順は `playbook/` にある。全体像は `playbook/README.md`。
- 既存の研究エンジン（`main.py` / `analyzers/` / `generators/`）は案件分析・投稿生成に使う下層ツール。
  playbook はその上の「月50万を12ヶ月安定させる運用レイヤー」。

## ① 扱っているジャンル
- 第1の実装ジャンル: **転職・キャリア**（高単価ジャンルの1つ。既存の note/SNS 資産あり → `outputs/`）
- 追加候補: `要確定`（テク1 Niche Sniper / テク4 Demand Index で選定する）

## ② 主要ペルソナ
- 既存ペルソナ（転職）: 20代女性会社員。「今じゃない」を3年繰り返し、転職したいのに動けない。
  日曜夜に求人サイトを開いて月曜に閉じる。静かで自然なトーンを好む。煽り・説教・自己啓発臭を嫌う。
- 詳細は `data/account_profile.json` と `outputs/sns_profiles_confirmed.md`。
- 高単価用の精緻ペルソナ（テク5/6）: `要確定`

## ③ 稼働中の案件と単価
- `要確定`（テク2 ASP Cross-Crawl で確定。確定したら「案件名 / ASP / 単価 / 承認率」を列挙）

## ④ NGの訴求軸（このアカウントで絶対にやらないこと）
- 「ボタン1つで月◯万」系の誇大・煽り表現
- 押し売り感の出るCTA（「プロフィールのリンクから今すぐ」等）
- 説教・自己啓発臭・上から目線
- 薬機法/景表法に触れる断定表現（第9章テク36で必ずチェック）
- 体験を伴わない一般論だけの記事（E-E-A-T欠落。テク11で補強）

## トーンの大原則
- 体験告白 > 情報提供。「長くなったのでnoteに残しました」レベルの自然さ。
- 主語は省略優先。「私」は最小限、「俺」は使わない。
- どのSNSから来ても「この人、自分と同じことを考えてた」と感じる一貫性（アンカー: 「今じゃない」3年 / 動けない / 整理）。

## 検証結果ログ（テク40 / 第5章の勝敗をここに積む）
- （まだなし。バリアント検証の勝者と数値をここに追記していく）

---
### セッション開始時のお願い
作業前にこのファイルの①〜④とトーンの大原則を確認してから始めること。
値を更新したら、このファイルも一緒に更新する。
