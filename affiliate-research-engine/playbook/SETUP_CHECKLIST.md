# フル自動に必要なもの — 逆算リスト（あなた側のアクション）

決定事項（2026-06-03）:
- **路線で分ける**: Track A=静かな転職/OL（温存）／Track B=別の高単価ジャンルで煽り系（新規）
- **ジャンルはまずテク3で決める** → 実行済み: `workspace/reports/genre-decision_LTV.md`
- **自動運転はフル**（Routines 5本すべて）
- **全部やりたい → 必要物を逆算して提示**（このファイル）

playbook の40プロンプトは**今すぐ手で使える**。以下は「フル自動運転」を成立させるための取得物。
**ゴール（Routines5本）から逆算**して、何が無いと何が止まるかを並べた。上から取れば最短で全部動く。

---

## 取得順（これを上から埋めれば、フル自動が完成する）

### ① Claude Code の `/schedule`（Routines）が使える環境 ★全自動の大前提
- これが無いと第8章の5本が1本も動かない。クラウド側で動くので最優先。
- 取得: Claude Code on the web / 対応プラン。→ 持っているか教えてください。

### ② ASPアカウント（A8 / もしも / afb / アクセストレード / バリューコマース 等）★Track Bの起点
- これが無いと **テク2が回らず、ジャンルも案件も確定しない**（=テク31の記事ネタが決まらない）。
- 最低1つ。複数あると特単比較ができる。→ 1つ作って、扱いたい候補ジャンルを教えてください。
- 完了で: `genre-decision_LTV.md` のテク1を実承認率で再採点 → Track Bジャンル確定 → CLAUDE.md ③へ。

### ③ Slack（Bot Token + Team ID）★Routines5本すべての通知先
- 無くても保存はされるが、「机に届く」感が出ない。Bot作成→トークン2つ。
- 完了で: テク31〜35の⑤通知が有効化。

### ④ Notion（アカウント + 「案件管理」DB + Integration Token）★テク34(月次P&L)に必須
- 無いと月次レポートが手元CSV頼みになる。DBを1つ作ってIntegration共有。
- 完了で: テク30/34が稼働。承認率下落の自動検知（テク38接続）も。

### ⑤ X予約投稿ツール（Buffer / SocialDog 等 + API）★テク32(SNS自動)の送信先
- 無いと生成までは自動だが「投稿」が手動。APIキー発行。
- 完了で: テク32が予約まで自動。

### ⑥ GA4（プロパティ + データ連携 or 定期エクスポート）★テク35(不調検知)の入力
- 無いと不調記事検知ができない。サイトにGA4を入れる→連携 or CSV吐き出し。
- 完了で: テク35が稼働。

### ⑦ Firecrawl APIキー（任意・強く推奨）★テク27/8/9を高速化
- 無くても手で代替可。あると競合LP/SERP分析が一気に速い。
- （動画も扱うなら ⑧ Supadata APIキー）

---

## いま動かせること（取得を待たずに今日）
- [x] 40プロンプト（`prompts/01〜10`）・CLAUDE.md・workspace 一式
- [x] テク3 LTV逆算 実行済み → `workspace/reports/genre-decision_LTV.md`
- [ ] **次の一手: ②ASPアカウントを1つ作る** → これがフル自動の律速。ここが空くと全部が前に進む。

---

## こちらへ教えてほしいこと（埋まり次第、実値で設定する）
1. ①の `/schedule` 環境は使える状態？
2. ②ASPはどれを作る／既にどれを持っている？ Track Bで攻めたい候補ジャンルは？
3. ③〜⑥のうち、すでに持っているアカウントは？（Slack / Notion / 予約投稿ツール / GA4）
4. ⑦Firecrawl等のAPIキーは取得可能？

→ ②が決まれば Track B のジャンル確定まで一気に進めます。③〜⑦は持っているものから順に `.mcp.json` と Routines を実値で設定します。
