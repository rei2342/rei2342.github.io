# Failure Detection Loop（テク35） — 2026-07-17

## ① GA4エクスポートCSVの確認

`affiliate-research-engine/playbook/workspace/reports/` を確認したが、GA4エクスポートCSVは存在しなかった
（既存ファイルは `genre-decision_LTV.md` / `genre-decision_TrackB.md` のみ。CSVは0件）。
リポジトリ全体を検索してもGA4/アクセス解析系のCSVは見つからず、アクセス解析MCP/APIも本セッションには
接続されていない（接続済みMCPはGoogle DriveとGitHubのみ）。

→ **データなし**

## ②〜④ 下落記事の抽出・原因分析・優先度付け

①でPV/CVデータが取得できなかったため、90日比PV下落記事の抽出・原因仮説・改修方向性・工数見積・
優先度付けは**スキップ**（対象データが存在しないため実施不可）。

## 次回実行に向けて

このルーチンを機能させるには、以下いずれかが必要:

1. GA4のエクスポートCSV（過去90日以上、記事URL単位のPV推移を含む）を
   `affiliate-research-engine/playbook/workspace/reports/` に配置する
2. アクセス解析MCP/API（GA4 Data API等）をセッションに接続する

上記いずれかが揃い次第、次回の実行で②〜④（下落記事抽出 → 原因仮説・改修方向性3つ・工数見積 →
下落幅×収益寄与での優先度順ソート → 本ファイル形式での保存）を実施する。
