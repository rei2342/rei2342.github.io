# Weekly SERP Watcher — 2026-07-27（未実施・要ブロッカー解消）

## 結論
今回の実行では、①〜⑥の本来の作業（実際の検索順位取得→下落抽出→差分分析→リライト候補→
`keywords.csv` 更新）を**実データで完了できなかった**。理由は下記3点のブロッカーで、
いずれも推測・仮値で埋めると `last_rank`/`checked_at` が事実と異なる数値として
今後の意思決定（リライト優先度、収益試算）に使われてしまうため、**あえて更新せず本レポートで報告する**。

## 見つかったブロッカー

1. **`keywords.csv` の `target_url` 列が全59行とも空欄**
   - 自社のどの記事のURLの順位を追うべきかが台帳上で特定できない。
   - 順位取得の前提（「このURLがこのキーワードで何位か」）が成立しない。

2. **順位取得手段（SERP API / ランクトラッキングMCP）が未接続**
   - `playbook/mcp/mcp.example.json` にあるのは Firecrawl / Supadata / Memory / Notion / Slack の
     テンプレートのみで、いずれも実キー未設定・未接続（`playbook/mcp/SETUP.md` 記載どおり）。
   - 順位取得専用のMCP/APIはテンプレートにも存在しない。手入力の運用（README脚注参照）だが、
     今回は人手入力も行われていない。

3. **記事の公開先（ライブサイト）が見当たらない**
   - `playbook/workspace/drafts/` にある記事はドラフトのMarkdownのみで、公開URLが確認できない。
   - このリポジトリ（`rei2342.github.io`）自体は無関係のINTJ系ページ（`intj*.html`）をホストしており、
     Track B（英語コーチング・留学）の記事はどこにも公開されていない。
   - `SETUP_CHECKLIST.md` を見る限り、GA4連携やドメイン確保もまだ「取得待ち」の状態。

## 今回とらなかった行動
- `keywords.csv` の `last_rank` / `checked_at` は**更新していない**（実測値がないため）。
- 順位・下落幅を仮定した「リライト優先度レポート」は**作成していない**（架空データになるため）。

## 次にこの部屋を動かすために必要なもの（`SETUP_CHECKLIST.md` と同じ逆算）
1. Track Bの記事を実際にどこかのドメインに公開する（GitHub Pagesでも可）。
2. `keywords.csv` の `target_url` に公開後の記事URLを埋める。
3. 順位取得手段を用意する（例: SERP API系MCP、または当面は Google Search Console / 手動チェックの値を
   `last_rank` に人力転記する運用）。
4. 上記が揃った時点で、本Routine（weekly-serp-watcher）を再実行すれば①〜⑥が実データで完了できる。

---
*このレポートは自動実行（scheduled task）により生成。人間の承認は経ていません。*
