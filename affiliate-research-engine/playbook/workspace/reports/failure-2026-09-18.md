# Failure Detection Loop — 2026-09-18

テク35 定期ルーティン（`playbook/routines/failure-detection-loop.md`）の実行記録。

## ① データ取得

- **アクセス解析MCP/API**: このセッションに接続なし（GA4/Search Console等のMCPツールは利用不可）。
- **手元エクスポートCSV**: `playbook/workspace/reports/` を確認したが、GA4エクスポートCSVは無し
  （同ディレクトリのCSVは `CONTENT_AUDIT_2026-08-10.csv` のみで、これは内容監査用。GA4エクスポートではない）。

→ **データなし。②③をスキップする。**

### 参考（今回の判定には使っていない）

`playbook/workspace/ga4/` に日次スナップショット（`.md`、直近14日ローリングのセッション/流入元/着地ページ集計）が
2026-08-06〜2026-09-17の45日ぶんあるが、これは
- CSVエクスポートではなく集計済みMarkdownであり、記事別の日次PV時系列としては使えない
- **GA4プロパティ自体が2026-08-06新規作成**（CLAUDE.md記載）で、それ以前のデータは存在しない。
  今日（2026-09-18）時点でも取得可能な最長期間は43日分であり、
  ルーティンが要求する「過去90日比」の比較は**データが存在しても2026-11-04頃まで成立しない**。

したがって、たとえ日次スナップショットをCSVとして`reports/`に配置しても、
現時点では90日比のPV下落記事抽出という②の条件を満たせない。

## ② 過去90日比PV下落記事の抽出

未実施（①がデータなしのため）。

## ③ 原因仮説・改修方向性・工数見積

未実施（①がデータなしのため）。

## ④ 優先度順まとめ

該当なし。

## 次回に向けて（恒久対応の提案）

1. GA4のMCP/APIをこのセッションから利用可能にするか、`gsc-reader@pelagic-media-504503-i9.iam.gserviceaccount.com`
   経由で記事別・日別PVのCSVエクスポートを生成し `playbook/workspace/reports/ga4_export_<日付>.csv` に置く。
2. 90日比較は **2026-11-04以降** に実施可能になる見込み（GA4プロパティ作成日2026-08-06から90日後）。
   それまでは比較期間を「取得可能な最長期間（現在43日）」に縮めて運用するか、
   このルーティン自体を90日データが揃うまで週次スキップにする運用が現実的。
