# Weekly SERP Watcher — 2026-08-03（未実行 / ブロック）

## 結論
今週の順位チェック・下落抽出・リライト候補レポート作成は**実行できなかった**。
`keywords.csv` の更新（last_rank / checked_at）も行っていない（実データがないため）。

## ブロック理由
1. **順位取得手段が存在しない**。このセッションに接続されているのは汎用WebSearchのみで、
   Google検索順位を返すAPI/MCP（SerpAPI・DataForSEO・Google Search Console等）は未接続。
   `weekly-serp-watcher.md` のルーティン定義自体が「順位は手入力か順位取得MCP/API」と
   前提を保留した状態のままだった。
2. **target_url列が84行全て空**。順位を調べる対象URL（`sakura-eigo.com`の各記事パス）が
   keywords.csvに記録されておらず、どの記事の順位を見ればいいか特定できない。
3. **last_rank列も84行全て空**。前週比較のベースラインが存在しないため、
   「5位以上下落」の判定はそもそも計算不能（引き算する片方の値がない）。

汎用WebSearchで「サイト名 + キーワード」を検索して結果に出てくるかどうかを見る方法もあるが、
それはGoogleの実際の掲載順位ではなく検索結果を代替推定するだけなので、
順位データとして`keywords.csv`に書き込むと**実態と異なる数値が一人歩きする**リスクがある。
そのため今回は数値の書き込みを見送った。

## 参考: このプロジェクトの直近の判断
`CLAUDE.md`の検証結果ログ（2026-08-01 反応分析）でも「SEOは死んでる（7月42imp/0click）。
伸びてるのはThreads→note→アフィ記事のソーシャル導線」「imp値が小さくSEO順位は参考程度。
主シグナルはソーシャル反応」と結論づけ済み。順位監視ルーティンの優先度自体を
見直す余地がある。

## 次にやること（どちらか）
- **(A) 順位監視を本気で回す場合**: SerpAPI/DataForSEO等のAPIキーを`.env`に追加するか
  Search ConsoleのMCP/コネクタを接続し、`keywords.csv`にtarget_url（記事URL）を埋める。
  その上で本ルーティンを再実行すれば①〜⑥が実データで回せる。
- **(B) 現状の方針通りSEOを参考程度に留める場合**: このルーティンの実行頻度を落とすか、
  Threads/note反応分析（`reaction-analysis_*.md`）ベースの監視に一本化する。

いずれか判断してから再実行する。
