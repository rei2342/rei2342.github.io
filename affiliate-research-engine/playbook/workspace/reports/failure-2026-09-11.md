# Failure Detection Loop（テク35）— 2026-09-11

## ①データ取得

`affiliate-research-engine/playbook/workspace/reports/` 配下（`daily/` `prepublish/` を含めて再帰的に確認）を確認した。

- GA4エクスポートCSV: **データなし**（見つからず）
- 見つかったCSVは `CONTENT_AUDIT_2026-08-10.csv` のみ。列は `id,bucket,score,title,impressions,clicks,position,...` で、
  Search Console由来の指標（impressions/clicks/position）と記事監査結果であり、
  **GA4のPV/日付系列ではない**ため、本ルーチンの入力として使えない。

参考までに確認した近傍データも、今回の判定には使えなかった:

- `playbook/workspace/ga4/*.md`: GA4取得結果はあるが、**日次スナップショット（サイト全体の流入元集計）のみ**で、
  記事別PVの時系列を持たない。また `CLAUDE.md` の記載どおり「記事別GA4はこのコンテナから取得できない」ため未取得。
- `playbook/workspace/reports/daily/snapshots/sakura.json`: GA4はこのsnapshotの対象から**意図的に除外**
  （`data_sources.excluded.google_analytics_4` = "Ray側が直接取得しているので二重に持たない"）。

**構造的な制約（記録として残す）**: `CLAUDE.md` によれば GA4プロパティ（548880365）は **2026-08-06に新規作成**され、
「この日より前のデータは存在しない」。本日 2026-09-11 時点でも取得可能な期間はまだ約36日分しかなく、
CSVが手元に置かれていたとしても**「過去90日比」の比較は前提として成立しない**（過去90日分のベースラインが存在しない）。

## ②〜③ 下落記事の抽出・原因仮説・改修方向性・工数見積

**スキップ**（①でデータなしのため実施せず）。

## ④ 優先度順の保存

本ファイルに「データなし」の記録のみを保存する。優先度リストは作成しない。

## 次回に向けて

- 記事別GA4データを本ルーチンで使うには、`playbook/workspace/reports/` に手動エクスポートCSV
  （記事URL×日付×PVなど）を置く運用に切り替えるか、記事別GA4取得の仕組みをこのコンテナ側にも用意する必要がある。
- 90日比較を行うには、GA4作成日（2026-08-06）から数えて最短でも2026-11-04以降まで待つ必要がある
  （厳密な「過去90日 vs さらに前の90日」の比較には2026年内は届かない）。
