# Failure Detection Loop（テク35） — 2026-08-28

## ① GA4エクスポートCSVの確認

`playbook/workspace/reports/` を確認した。GA4のエクスポートCSVは**無い**。

- ディレクトリ内のCSVは `CONTENT_AUDIT_2026-08-10.csv` の1件のみ。中身は
  `scripts/content_audit.py` が作った記事監査結果（GSC由来の impressions/clicks/position 等）で、
  GA4のPV/CVエクスポートではない。
- 参考: `playbook/workspace/ga4/` に `ga4_fetch.py` が毎日出している集計 `.md` はあるが、
  ①これは `reports/` の外にある、②CSVエクスポートではなくMarkdown要約、
  ③直近14日ローリングのみで90日比較に使える形式ではない。

**→ 「データなし」と記録し、以降の抽出処理はスキップする。**

## ② 過去90日比PV▲30%以上の記事抽出

**未実施（対象データなしのため実行不可）。**

なお `playbook/workspace/ga4/` の日次スナップショットを参考情報として見ると、
GA4プロパティ（`548880365` / sakura-eigo）自体が **2026-08-06新規作成**で、
本日（2026-08-28）時点でまだ**22日分のデータしかない**（CLAUDE.mdに明記の既知事実）。
そのため、CSVが仮に手元にあったとしても、90日比較は分母となる「90日前の期間」が
まだ存在せず、原理的に成立しない。記事単位のPVも直近14日で1〜3PV程度と極小で、
「30%下落」を検出しても季節性/母数不足によるノイズと実質下落を区別できない状態にある。

## ③ 原因仮説・改修方向性・工数見積

**対象記事なし（②が未実施のため）。**

## ④ 優先度順の保存

本レポート自体が④の成果物。優先度付き記事リストは**0件**。

---

## 次回に向けて（やること）

- GA4のCSVエクスポートを `playbook/workspace/reports/` に置く運用を始めるか、
  もしくは `ga4_fetch.py` の出力先を `reports/` 配下にも複製するかを決める
  （現状は `playbook/workspace/ga4/` のみで、本ルーチンの読み込み対象外）。
- 90日比較が可能になるのは GA4プロパティ作成日（2026-08-06）から90日後、
  **2026-11-04以降**。それまでは本ループを回しても「データなし」が続く見込み。
- それまでの代替シグナルとして、Search Console（`workspace/gsc/`）や
  Threads遷移率（`workspace/threads_insights/`）など、稼働期間が長い指標で
  反応の悪い記事を見ておく方が実効性がある可能性（次回セッションで検討）。
