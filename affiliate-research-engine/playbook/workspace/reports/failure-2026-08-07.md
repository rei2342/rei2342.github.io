# 記事PV下落分析 2026-08-07

## ①GA4エクスポートCSVの確認

`playbook/workspace/reports/` にGA4エクスポートCSVは**存在しない**（.mdレポートのみ）。
`playbook/workspace/ga4/` には過去のGA4取得結果（`LATEST.md` / `2026-08-06.md`）があるが、
これはAPI直接取得のサマリmdであり、指定の「reports/配下のエクスポートCSV」ではない。

→ **データなし。①の時点でスキップする。**

## 90日比較そのものが原理的に成立しない理由

`ga4/LATEST.md`（2026-08-06取得）によれば:

- GA4プロパティ `548880365`（sakura-eigo）は**過去365日の合計セッションが0**
  （＝計測開始以来、記事別PVのデータが一度も入っていない）
- `CLAUDE.md` 記載のとおり、このGA4プロパティは**2026-08-06に新規作成**されたばかりで、
  「タグを入れた時点より前のデータは存在しない」
- 本日（2026-08-07）時点でGA4計測開始から**まだ1日強**しか経過していない

→ 「過去90日比でPVが30%以上下落した記事」を抽出するための前提データ（90日分のGA4記事別PV）が
そもそも存在しない。CSVが見つかったとしても、期間が足りず90日比較は不可能な状態。

## ②③④

①でデータなしのため、下落記事の抽出・原因仮説・改修方向性・工数見積・優先度付けは
**すべてスキップ**。

## 次にこの分析を回せる条件

- GA4の記事別セッション/PVが**90日分蓄積される**（最短でも2026-11月上旬以降）
- それまでの代替シグナルは `playbook/workspace/gsc/`（Search Console）の検索語・順位データ。
  ただし「SNSからの記事到達」はSearch Consoleでは追えない（GA4専用領域）ため、
  Threads/X経由の下落はこの分析の対象外になる点に注意
- それまでは本レポートのような「データなし」記録を都度残し、GA4蓄積量の進捗を追跡する
