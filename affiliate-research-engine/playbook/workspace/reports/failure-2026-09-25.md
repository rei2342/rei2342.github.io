# Failure Detection Loop 2026-09-25

テク35（反応が悪い記事の自動検知）の定期実行。今回は**分析未実施**（理由は①参照）。

## ①GA4エクスポートCSVの確認

`playbook/workspace/reports/` を確認したが、GA4エクスポートCSVは**データなし**。
存在したのは `CONTENT_AUDIT_2026-08-10.csv`（コンテンツ監査用。GSC由来の
impressions/clicks/positionのみでPVは含まない）と、サブディレクトリ
`daily/`（`snapshots/sakura.json` のみ）・`prepublish/`（下書き点検メモのみ）。
いずれもGA4エクスポートではない。

→ **記録: データなし。CSV読み込みはスキップ。**

### 参考: 代替になり得なかった既存データ

- `playbook/workspace/ga4/*.md`（2026-08-06〜2026-09-24の日次取得）は存在するが、
  ①各日「直近14日ローリング集計のページ別上位20件」であり、記事ごとの
  日次PVを積み上げた時系列ではない。②GA4計測は**2026-08-06開始**（CLAUDE.md記載の
  通りこれより前のデータは存在しない）ため、今日時点で経過**50日**しかなく、
  「過去90日」を切り出す基準期間そのものが存在しない。
  このため②のPV下落抽出の代替データとしては使えないと判断した。
- Search Console（`playbook/workspace/gsc/`）は検索経由の指標であり、
  CLAUDE.md の運用ルール上「GSCとGA4は別データとして持ち、混ぜて1つの
  結論にしない」ため、GA4 PVの代用にしなかった。

## ②〜④ 下落記事の抽出・原因仮説・優先度付け

**実施不可。** ②はGA4の「過去90日 vs その前90日」のPV比較を前提にしており、
①で必要なデータ（CSVまたは同等の記事別・日次PV時系列）が確認できなかったため、
実データに基づく下落記事の抽出・原因仮説・工数見積・優先順位付けは行っていない。

数字を仮置きして分析結果を作ることはしない
（CLAUDE.mdの最上位ルール「根拠のない事実・数値を生成しない」の精神に従う）。

## 次回に向けて（対応案）

1. **CSVエクスポートを置く運用にする場合**: GA4管理画面から
   「記事URL × 日別PV」を90日以上の期間でエクスポートし、
   `playbook/workspace/reports/` に置けば次回①で読み込める。
2. **既存のAPI取得（`playbook/scripts/ga4_fetch.py`）を使う場合**: 現状は
   「直近14日ローリング・上位20件」形式のため、記事ごとの日次PVを
   蓄積する形式（例: 日付×記事URL×PVのロングフォーマットCSV/JSONを
   `workspace/ga4/`に追記していく）に変更する必要がある。
3. **データ蓄積を待つ場合**: GA4計測開始が2026-08-06のため、
   「過去90日」が実在するのは早くても**2026-11-04以降**。
   それまでは本ルーチンを回しても①で毎回「データなし」になる見込み。

## 優先度順リスト

該当データなしのため、今回は空。
