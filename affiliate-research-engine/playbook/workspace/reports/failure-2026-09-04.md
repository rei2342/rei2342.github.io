# Failure Detection Loop — 2026-09-04

テク35「反応が悪い記事を自動検知して改修案を出す」の実行結果。

## ① データ取得

`affiliate-research-engine/playbook/workspace/reports/` を確認したが、**GA4エクスポートCSVは存在しない**。

- 同ディレクトリにあるCSVは `CONTENT_AUDIT_2026-08-10.csv` のみで、これはGSC指標（impressions/clicks/position）＋台帳監査であり、GA4のPVエクスポートではない。
- GA4関連データとしては `playbook/workspace/ga4/*.md`（日次スナップショット、CSVではなくMarkdown要約）が存在するが、`CLAUDE.md` の記載により **GA4プロパティ（548880365）は2026-08-06新規作成で、この日より前のデータは存在しない**。本日（2026-09-04）時点で最大でも直近29日分しか蓄積されておらず、90日比較に必要な「90日前時点のPV」が構造的に取得不能。

→ **データなし。以降のステップはスキップする。**

## ② 過去90日比でPVが30%以上下落した記事

対象記事なし（①の理由により算出不可）。

## ③ 原因仮説・改修方向性・工数見積

該当なし。

## ④ 優先度順リスト

該当なし。

## 備考・次回に向けて

- 90日比較が可能になるのは GA4計測開始（2026-08-06）から90日後、**2026-11-04以降**。それまでは本ループの②③④は実行不可。
- 代替として、直近29日以内の短期変動（例: 週次比・14日比）であれば `playbook/workspace/ga4/LATEST.md` 等の日次スナップショットから追える可能性がある。90日固定の比較条件を緩めるかどうかは要判断（`playbook/routines/failure-detection-loop.md` のプロンプト変更が必要）。
- GA4の記事別PVを継続的にCSVエクスポートして `./workspace/reports/` に置く運用が無い場合、次回以降も①で毎回止まる。エクスポート運用を整えるか、GA4 API取得スクリプト（`playbook/scripts/ga4_fetch.py`）の出力を記事別・日別の構造化データ（CSV/JSON）として保存するかを検討する。
