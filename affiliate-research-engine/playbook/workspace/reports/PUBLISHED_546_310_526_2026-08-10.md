# 反映後報告｜546・310・526（2026-08-10）

**3本とも公開反映まで完了。保留0件。**

| | 546 | 310 | 526 |
|---|---|---|---|
| URL | `/online-english-lessons-90-days-success/` | `/cheap-english-learning-apps-free/` | `/fast-verify-cost-time-travel/` |
| 反映時刻(GMT) | 02:47:26 | 02:47:30 → 02:52 | 02:47:32 → 02:52 |
| 本文 | 2,993 → **3,659字** | 3,202 → **3,462字** | 1,572 → **2,630字** |
| H2 | 7 → 7 | 7 → 7 | 6 → **7** |
| CTA | 1 → 1 | 2 → 2 | 1 → 1 |
| 差分 | +46 / −4 行 | +36 / −13 行 | +50 / −12 行 |

---

## 1. 公式再確認結果

`workspace/market/official/2026-08-10.yaml`（Actions で取得。取得日時つき）

| 案件 | 取得 | 確認できたこと | 記事での使い方 |
|---|---|---|---|
| スパトレ | 11:34:37 | **「7日間無料トライアル実施中！」を原文で確認**。`free_period=7` | **CTAアンカー「7日間無料体験を見る」を維持**（指示の分岐①） |
| ネイティブキャンプ | 11:34:32 | `free_period=7` / `count=回数無制限` / 原文抜粋あり | CTAアンカー「予約不要・レッスン回数無制限」を維持 |
| speek | 11:34:36 | ページは200。**料金・無料期間・回数は抽出できず**（`null`） | **監修者名・所属は新規追加していない。** 既存段落は文言も位置も変更なし |
| フィリピン留学ナビ | 11:34:34 | ページは200。原文抜粋あり（語学留学は無料） | 既存の公式情報段落を維持 |
| ネイティブキャンプ留学 | — | **不要になった**（下記） | — |

> **ネイティブキャンプ留学は照合対象から外しました。**
> 最初 `https://nativecamp.net/ryugaku` を叩いて **HTTP 404**。
> 正しい着地先（`/study_abroad/counseling-reserve`）へ直したうえで
> 公開中の526本文を確認したところ、**そもそもこの案件は526に載っていません**でした。
> 詳細は「4. 保留した記事と理由」の下の注記。

### CTA判定の適用

指示の3分岐のうち、**①「7日間が再確認できた → 維持」**に該当。
310のスパトレCTAは文言を変えていません。

### リンク先の HTTP と最終URL（反映後）

| 記事 | CTA | HTTP | 最終URL のホスト |
|---|---|---|---|
| 546 | ネイティブキャンプ | **200** | `nativecamp.net` |
| 310 | speek | **200** | `speek.jp` |
| 310 | スパトレ | **200** | `sptr.jp`（`?maf=2409_5640981.31559…`） |
| 526 | フィリピン留学ナビ | **200** | `philippines-university.jp`（`?rd_code=…`） |

内部リンク **13本すべて HTTP 200 / noindex なし**。

> ⚠️ **最初の計測では moshimo の3本が404でした。あれは私の検査バグです。**
> WordPress は `&` を `&#038;` で保存しており、実体参照を戻さずに叩いたため
> クエリが壊れていました。`html.unescape()` を入れて再計測したところ、
> 3本とも200で正しい着地先へ飛びます。**リンクは生きています。**
> （a8のリンクだけ最初から200だったのは、URLに `&` が無かったためです）

---

## 2. 残るNG3件の分離（検査側の修正）

**最終本文のNGは3本とも0件。「既知の誤検知」として残していません。**

| 指示 | 実装 | 効果 |
|---|---|---|
| CTAアンカーの無料・期間・料金は `cta_claims` へ送る | `mask_link_anchors()` で CTAアンカーを本文検査から除外。照合は `cta_claim_gate` と `cta_claims.csv` | 546/310の `experience_claims` 誤検知が消えた |
| 内部リンクのアンカー文の点数・金額・期間は一人称検査から外す | 同上でマスク。かわりに **`internal_link_gate()`** を新設し、**HTTP 200 / アンカーとタイトルの対応 / 公開 / noindex** の4点を検査 | 310の `fact_claims`（「TOEIC 600点」）誤検知が消えた |
| 市場表現の引用と、第三者との実際の会話を分ける | `is_market_voice()`。**引用符の一律除外はしない。** 受け手（`私に`/`講師から`）・発言者・時制で判定 | 526の `unverified_self_facts`（「『早い』と言われ」）誤検知が消えた |

検証したケース（`quality_rules`）:

- 「『早い』と言われている」→ 市場表現（通す）
- 「私は講師に『向いていない』と言われた」→ **実際の会話（落とす）**
- 「カウンセラーから『間に合わない』と言われた」→ **落とす**
- 内部リンクのみの「TOEIC 600点」→ 通す ／ 本文の「私はTOEIC 600点を取った」→ **落とす**
- CTAのみの「7日間無料体験」→ 通す ／ 本文の「スパトレを3ヶ月使った」→ **落とす**

**4件目も見つけて直しました。** 546で
「公式情報：ネイティブキャンプは…7日間の無料体験があると案内されている（公式サイト・2026年8月8日時点）」
が体験表現として拾われていました。**出典と確認日が同じ文にあるサービス説明は体験ではない**ので、
文まるごとで出典を見て外すようにしました（`SOURCED_SPEC_RE`）。
「スパトレを3ヶ月使った」のような一人称の利用は引き続き落ちます。

---

## 3. 反映した記事

**546・310・526 の3本すべて。**

### 見出しの変更前後

**546**（タイトル・slug・主要見出しの文言はすべて維持）

| | 変更前 | 変更後 |
|---|---|---|
| H2 ×7 | 7本すべて | **文言・本数とも変更なし** |
| 新規H3 | — | まず、やめた瞬間はどちらでしたか |
| 新規H3 | — | A｜授業中で止まっていた場合に見直すもの |
| 新規H3 | — | B｜予約の前で止まっていた場合に見直すもの |

**310**

| | 変更前 | 変更後 |
|---|---|---|
| H2 | 無料期間中に確認する項目 | **無料で試せる期間に、この5つだけ書き出す** |
| 新規H3 | — | 1日ぶんの記入欄 |
| 新規H3 | — | 解約期限と次回の請求日 |
| 他のH2 ×6 | — | 変更なし |

**526**

| | 変更前 | 変更後 |
|---|---|---|
| H2 | 費用と時間を、候補ごとに並べる | **費用と時間を、混ぜずに並べる** |
| 新規H2 | — | この出し方が向かない場合 |
| 新規H3 | — | 第1カード｜数えられる3つ |
| 新規H3 | — | 第2カード｜このカードからは出せないもの |
| 他のH2 ×5 | — | 変更なし |

### 本文の差分要約

完全な行差分: `workspace/rewrites/published_diff_{546,310,526}.txt`

**546**（+46 / −4行）
- 冒頭2段落を、意志を責めない代弁＋受け止めへ
- 2択診断（A/B）と、Aの見直し3項目、Aから抜ける内部リンク2本
- 「続けるコツ」節の冒頭に、何と対比しているかの1文
- CTA直前に「予約の前で閉じていた人だけ」の1文
- **削除は旧2項目リストのみ。** 中身はA/Bに全部移した

**310**（+36 / −13行）
- 導入に受け止めの1文
- 機能説明4段落を各1文へ圧縮（−9行相当）
- 5項目の箇条書き →「1日ぶんの記入欄」5行＋「解約期限と次回の請求日」2行
- CTA直前に条件の1文（2本とも）

**526**（+50 / −12行）
- 冒頭に、検索結果の前提が揃っていない注記（確認日つき・**金額は書いていない**）
- 4項目の表 → 第1カード（3つ）と第2カード（上達の速さは出せない）
- 「この出し方が向かない場合」4項目を新設
- 「語学留学であれば年齢条件がない国もある」を**削除**し、確認手順へ
- CTA直前に条件の1文

### スマホ表示形式

**3本とも `<table>` を1つも使っていません**（公開画面で `表 0`）。
`<pre>` 0 / `white-space:nowrap` 0。横スクロールの原因になる要素はありません。
記入欄もカードも、見出し＋箇条書きなのでPCとスマホで同じ形で読めます。

### CTA変更前後

| 記事 | 変更前 | 変更後 |
|---|---|---|
| 546 | 1本・ネイティブキャンプ・CTAボックス内 | **1本のまま。** 位置も同じ。**直前に「予約の前で閉じていた人だけ」の1文を追加**。Aの分岐にはCTAを置かず、内部リンク2本で完結 |
| 310 | 2本・speek / スパトレ・各H3末尾 | **2本のまま。** 案件・文言・位置とも変更なし。**各CTAの直前に条件の1文を追加** |
| 526 | 1本・フィリピン留学ナビ | **1本のまま。** **直前に「第1カードの費用総額が自分では出せない候補がフィリピンなら」の1文を追加** |

**アンカー文言は3本とも1文字も変えていません。** `cta_claims` 台帳と一致しています。

### 削除した未確認主張

| 記事 | 削除したもの | 理由 |
|---|---|---|
| 526 | **「語学留学であれば年齢条件がない国もある」** | 制度の条件の断定。国名も出典も無く、公式を取得していない。`institution_country_gate` が拾っていた |
| 310 | 「自分の発音を数値で見られるので、独学の目安になる」 | 効果の主張。公式で確認していない |
| 310 | 「あとから振り返る材料になるので、この記録は残しておく価値がある」 | 同上 |
| 310 | 「自分の弱点を自分で解釈しなくてよくなる」 | 同上 |
| 310 | 「自分で組み立てる手間がなくなるかわりに」 | 同上 |
| 546 | なし | — |

**新しく足した公式情報の主張は3本合計で0件です。**
金額・無料期間・回数・解約条件を1つも新規に書いていません。
526には**特定の費用相場を1つも書いていません**（幅が割れている事実だけを確認日つきで注記）。

> **「最低価格保証」について。** 削除対象として準備していましたが、
> 公開中の526本文には**もともと存在しませんでした**（ローカルの控えが古かった）。
> 現在の本文にも入っていません。

---

## 4. 保留した記事と理由

**保留 0件。3本とも反映しました。**

ただし、途中で計画を変えた点が2つあります。**どちらもローカルの控えが古かったことが原因**です。

1. **526の土台。** `workspace/claims/posts/310.raw.html` に、2026-08-09 13:58 の
   パッチで直したはずの旧断定が2件残っていました。控えを土台にしていたら、
   **直した内容を巻き戻して公開していました。**
   → 反映の直前に公開中の本文を取り直す `article-fetch` を作り、そちらを土台にしました。
2. **526のCTA構成。** 控えには「ネイティブキャンプ留学の公式情報＋a8のCTA」と
   「最低価格保証」がありましたが、**公開中の本文にはどちらもありません**でした。
   CTAはもともと1本（フィリピン留学ナビ）だけです。
   → 「2本→1本に減らす」ではなく「1本のまま位置づけを明示する」に変えました。

---

## 5. 全ゲート結果

反映後の本文に対して実行（`workspace/rewrites/gate_results.json` と Actions ログ）。

| ゲート | 546 | 310 | 526 |
|---|---|---|---|
| fact gate（unverified_self_facts） | **0** | **0** | **0**（反映前は1） |
| official gate（official_spec_without_source） | **0** | **0** | **0** |
| CTA gate（cta_claim_gate） | **0** | **0** | **0** |
| CTA gate（cta_box_text_gate） | **0** | **0** | **0** |
| institution / country gate | **0** | **0** | **0**（反映前は1） |
| price_without_basedate | 0 | 0 | 0 |
| hype_words | 0 | 0 | 0 |
| personal_plan | 0 | 0 | 0 |
| time_sensitive_fact | 0 | 0 | 0 |
| missing_caveat | 0 | 0 | **0**（反映前は1） |
| experience_claims | **0**（反映前は1） | **0**（反映前は1） | 0 |
| fact_claims | 0 | **0**（反映前は1） | 0 |

**NG合計 0件。** 反映前は5件（546:1 / 310:1 / 526:3）。

`test_social_spec.py` 失敗0件。

---

## 6. 公開画面の検証結果

`workspace/market/pagecheck/2026-08-10_1153.json`

| 項目 | 546 | 310 | 526 |
|---|---|---|---|
| HTTP | **200** | **200** | **200** |
| canonical 自己参照 | **True** | **True** | **True** |
| noindex | **なし** | **なし** | **なし** |
| タイトル反映 | **一致** | **一致** | **一致** |
| 文字化け（U+FFFD） | **0** | **0** | **0** |
| HTML開閉（p/h2/h3/ul/ol/li/a） | **正常** | **正常** | **正常** |
| 表（横スクロール要因） | **0** | **0** | **0** |
| CTA本数 | **1** | **2** | **1** |
| CTA最終URL | nativecamp.net (200) | speek.jp / sptr.jp (200) | philippines-university.jp (200) |
| 内部リンク切れ | **0**（5本） | **0**（4本） | **0**（4本） |

### 変更していないことの確認

| | 546 | 310 | 526 |
|---|---|---|---|
| slug | 変更なし | 変更なし | 変更なし |
| URL | 変更なし | 変更なし | 変更なし |
| 公開日 | 2026-08-07T11:30:06 のまま | 2026-08-02T14:05:50 のまま | 2026-08-05T20:32:49 のまま |
| 公開ステータス | publish のまま | publish のまま | publish のまま |
| 著者 | 変更なし | 変更なし | 変更なし |
| canonical | 自己参照のまま | 自己参照のまま | 自己参照のまま |

送信したのは `content` フィールドだけです
（`title` / `status` / `slug` / `date` / `author` は payload に入れていません）。
反映後に取り直して、これら6項目が変わっていないことを確認しています。

### 546へ入る内部リンク7本

**7本のリンク先はすべて有効です。**

- **URL・slug・タイトルを変更していない**ので、リンク先は変わりません
- 546のHTML内に `#` 付き（見出しアンカー）の被リンクは**0件**。
  見出しを指しているリンクはありません
- 546の**主要見出し（H2 7本）の文言を1つも変えていない**ので、
  仮に見出しアンカーがあっても壊れません

### 新旧見出し対応

上の「3. 反映した記事 → 見出しの変更前後」の3つの表が対応表です。
**変更したH2は 310 と 526 の各1本のみ。546は0本です。**

### 見つかった軽微な点（未対応・報告のみ）

- 546の内部リンク3本で、アンカー文とリンク先のタイトルが一致しません
  （例：アンカー「1回のレッスンで話した秒数を測る」→ タイトル「オンライン英会話で
  自分が話した秒数を、録音して…」）。**リンクは200で生きています。**
  説明的な言い換えなので実害はありませんが、`internal_link_gate` の
  `title_match` は「不一致」を返します
- 546で「1回のレッスンで話した秒数を測る」への内部リンクが2箇所になりました
  （Aの分岐と、末尾の「次に読むと決めやすいもの」）

---

## 7. バックアップ場所

| 時刻 | 場所 | 中身 |
|---|---|---|
| 2026-08-10 11:45 | `workspace/backups/2026-08-10_1145/` | **DRY RUN**。3本の `{id}.json`（記事オブジェクト全体）・`before.html`・`after.html` |
| 2026-08-10 11:47 | `workspace/backups/2026-08-10_1147/` | **本番反映時**。同じ3点セット × 3本 |
| 2026-08-10 11:51 | `workspace/backups/2026-08-10_1151/` | CTA文の位置直し（310・526） |

`{id}.json` は `context=edit` で取得した記事オブジェクトそのもので、
`content.raw` / `title` / `slug` / `status` / `date` / `author` /
`categories` / `featured_media` を含みます。

反映後の本文は `workspace/claims/live/{id}.html` にもあります。

## ロールバック方法

```bash
# Actions で rewrite-uploader.yml を workflow_dispatch
#   ref = claude/affiliate-research-engine-XQ0LF

# 1) 戻したい断面の before.html を復元用の置換指示にする
#    （before → after を逆向きにした apply_<id>.yaml を作る）

# 2) いちばん単純な戻し方：バックアップの content をそのまま送る
python - <<'EOF'
import json, requests
pid = 546
d = json.load(open(f"workspace/backups/2026-08-10_1147/{pid}.json"))
requests.post(f"https://sakura-eigo.com/wp-json/wp/v2/posts/{pid}",
              auth=("rei.00pt2342@gmail.com", WP_APP_PASSWORD),
              json={"content": d["content"]["raw"]})   # content だけ送る
EOF
```

WordPress のリビジョン機能でも戻せます（本文だけを更新しているので、
リビジョン一覧に 2026-08-10 02:47 / 02:52 の版が残っています）。

---

## 8. WordPress以外の変更

すべて `claude/affiliate-research-engine-XQ0LF` ブランチ。**mainへは push していません。**

| 種類 | 中身 |
|---|---|
| 検査の修正 | `quality_rules.py` … `mask_link_anchors` / `internal_link_gate` / `is_market_voice` / `SOURCED_SPEC_RE` を追加。`experience_claims` / `fact_claims` が html を受け取るように |
| 呼び出し側 | `content_audit.py` / `wp_audit.py` を新しい引数へ |
| 運用の口 | `social_ops.py` に `article-fetch` / `article-apply` / `link-check` / `page-verify` を追加 |
| 市場調査 | `market_research.py` の公式URL修正（`/ryugaku` は404）＋控えのコミット |
| 置換指示 | `workspace/social/patches/apply_{310,526}.yaml` |
| 取得した控え | `workspace/claims/live/`・`workspace/market/official/`・`workspace/market/links/`・`workspace/market/pagecheck/` |
| バックアップ | `workspace/backups/2026-08-10_{1145,1147,1151}/` |
| 差分 | `workspace/rewrites/published_diff_{546,310,526}.txt` |

---

## 9. SNS投稿0件・SNS承認0件の確認

| 項目 | 値 |
|---|---|
| SNS投稿（state=posted） | **0件** |
| SNS承認（state=approved） | **0件** |
| 在庫 | 10件すべて `awaiting_approval` |

今回の作業でSNS在庫の状態は1件も動かしていません。

> **なお、記事本文を変えたので `article_unchanged` ゲートの前提が変わりました。**
> 546・310・526 の `modified_gmt` が 2026-08-10 に更新されたため、
> これらを指すSNS在庫（X-546 / THREADS-546 / X-310 / THREADS-310 /
> X-526 / THREADS-526 の6件）は、**投稿前に stale へ戻して作り直す必要があります。**
> 承認前なので実害はありませんが、次に触るときの最初の作業です。
