# PV下落記事 分析レポート — 2026-08-21

## ① データ確認

`affiliate-research-engine/playbook/workspace/reports/` を確認したが、GA4エクスポートCSVは**見つからなかった**。

- 同ディレクトリに存在するCSVは `CONTENT_AUDIT_2026-08-10.csv`（コンテンツ監査台帳）のみで、GA4のページ別PV/セッションを含むエクスポート形式ではない。
- GA4データ自体は `playbook/workspace/ga4/*.md`（`2026-08-06.md` 〜 `2026-08-20.md`、日次サマリ）としては存在するが、これは `ga4_fetch.py` による**直近14日の集計Markdown**であり、指定された「reports/ のGA4エクスポートCSV」ではない。また直近14日合計でもサイト全体PVが77件（ほぼ `/` トップページに集中）と少なく、記事単位・90日粒度でのPV推移を比較できる形式のデータではない。

→ **データなし。②〜③（下落記事の抽出・原因仮説・改修方針）は実施不可のためスキップ。**

## ② 過去90日比PV▲30%以上の記事抽出

スキップ（①のため実施不可）。

## ③ 原因仮説・改修方向性・工数見積

スキップ（①のため実施不可）。

## ④ 優先度順サマリ

該当なし。GA4エクスポートCSVを `playbook/workspace/reports/` に配置後、再実行が必要。

### 次アクション（提案）
- GA4管理画面から記事URL別のPVを含むエクスポートCSV（過去90日以上、比較期間込み）を `playbook/workspace/reports/` に配置する。
- もしくは `playbook/scripts/ga4_fetch.py` を記事別・90日レンジで拡張し、CSV出力させる。
