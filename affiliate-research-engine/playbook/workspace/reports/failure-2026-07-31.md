# Failure Detection Loop — 2026-07-31

## ①データソース確認

`affiliate-research-engine/playbook/workspace/reports/` を確認したが、GA4エクスポートCSVは存在しない。
（存在したファイル: `genre-decision_LTV.md`, `genre-decision_TrackB.md` — いずれもPV/CVデータではない）

アクセス解析MCP/APIも本セッションには接続されていない。

**→ データなし。②〜④（PV下落記事の抽出・原因仮説・優先度付け）はスキップする。**

## 次回に向けて

- GA4から過去90日分のPV/CVエクスポートCSVを取得し、このディレクトリ（`./workspace/reports/`）に配置すると、次回実行時に本ルーチンが自動で読み込む。
- 恒久対応として、GA4 API/MCP接続を用意できれば手動エクスポートが不要になる（`playbook/routines/failure-detection-loop.md` 依存欄を参照）。
