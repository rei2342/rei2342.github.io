# Failure Detection Loop（テク35）— 2026-07-10

## ①データソース確認
- `playbook/workspace/reports/` を確認 → GA4エクスポートCSVは**なし**（既存ファイルは `genre-decision_LTV.md` / `genre-decision_TrackB.md` のみ）。
- アクセス解析MCP/APIも未接続（`playbook/mcp/SETUP.md` に定義されているのは Firecrawl / Supadata / Memory / Notion・Sheets / Slack のみで、GA4/analytics系MCPは未設定）。

→ **データなし。②〜④（下落記事抽出・原因仮説・優先度リスト）は実行不可のためスキップ。**

## 次回実行に向けて（要対応）
下落検知を回すには、次のいずれかが必要:
1. GA4のエクスポートCSV（過去90日 x 過去30日比較できる範囲、記事URL単位のPV/CV）を `playbook/workspace/reports/` に配置する。
2. または GA4 MCP/APIを接続する（`playbook/mcp/mcp.example.json` にGA4用の設定を追加し、`claude mcp add` で登録）。

対応後、本ルーチン（`playbook/routines/failure-detection-loop.md`）を再実行すれば、②下落記事抽出→③原因仮説・改修方向性・工数見積→④優先度順レポートまで通しで生成できます。
