# 最終修正の結果（2026-08-09）

**投稿は0件。** 承認は指示された3件だけです。

## 検算

| 項目 | 値 |
|---|---|
| supported | 34 |
| unsupported | **0** |
| unjudged | 24 |
| needs_human_review | 5 |
| 命題の合計 | 63 |
| X加重文字数 | 227 / 191 / 175 / 214 / 231（上限280）|
| 記事310本文の旧断定 残存 | 0 |
| 投稿 | 0件 |
| 承認 | 3件（X-23-a / THREADS-526-a / X-546-a）|

`unjudged` を `supported` に混ぜていません。前回の報告で
「unsupported 0件」とだけ書いたのは、**判定していないものを
支持されているものと同じ扱いにしていた**ためです。
分けたところ、63件のうち支持されているのは34件で、
24件は判定していません。

## 在庫の状態

| stock_id | 状態 |
|---|---|
| THREADS-23-a | awaiting_approval |
| THREADS-310-a | awaiting_approval |
| THREADS-521-a | awaiting_approval |
| THREADS-526-a | approved |
| THREADS-546-a | awaiting_approval |
| X-23-a | approved |
| X-310-a | awaiting_approval |
| X-521-a | awaiting_approval |
| X-526-a | awaiting_approval |
| X-546-a | approved |

## 人が見るもの（5件）

判定できない短文のうち、言い切りで、比較・因果・診断・効果・否定の
どれかを含むものです。**落としていません。**

| stock_id | 本文 |
|---|---|
| THREADS-521-a | 再生時間だけでは、意味を追えた回数が分かりません。 |
| THREADS-546-a | 予約の前で閉じていたなら、話し始めるまでの操作の数のほうかもしれません。 |
| X-310-a | 英語アプリは、月額だけでは選びきれません。 |
| X-546-a | オンライン英会話が続かないのは、意志の弱さとは限りません。 |
| X-546-a | 止まった場所によって、見るところが変わることがあります |

## 1〜8の対応

1. **`unjudged` を分けた。** 内容語3語未満は `ok` にせず `unjudged` に
   します。判定の順番も変えて、モダリティや極性を見る前に
   この分岐へ入るようにしました（以前は低い一致度の枝が先に走り、
   短文が `ok` になっていました）
2. **`needs_human_review` を足した。** assertive か hedged で、
   内容語3語未満で、比較・因果・診断・効果・全面否定のどれかを含むもの。
   落とさずに数え、REVIEWへ本文ごと出します
3. **報告を3数値に分けた。** supported / unsupported / unjudged。
   `needs_human_review` も併記します
4. **記事310を直した。** WordPress の本文を2か所、
   断定から確かめ方へ変えました（HTTP 200 / modified_gmt 2026-08-09T13:58:10）。
   旧断定の残存は0
5. **指定の本文へ差し替えた。** Threads-23 / X-310 / Threads-310 の2投稿 /
   X-521 / Threads-521 の2か所 / X-526 / Threads-546
6. **Threads-526・X-23・X-546 は本文を変えていません**
7. 上の検算のとおり
8. **X-23-a / THREADS-526-a / X-546-a を approved へ。** 残り7件は
   `awaiting_approval` のままです。自動投稿はしていません

## 指定の本文を通すために直したもの

指定された文が、こちらのゲートの粗さで落ちました。**本文は変えず、
判定のほうを直しました。** 直したのは4点です。

| 直したもの | なぜ |
|---|---|
| 語の照合を部分一致にした | 形態素解析が無いので「解約期限」と「解約」が別語になり、同じことを言っていても一致0になっていた |
| 記事側を1〜2文の窓で見る | 記事は1つの主張を2文に分けて書く（「〜時間にもできる。確認するのは次の5つ」） |
| 「確認項目」「提案」の札を弱い主張として扱うのをやめた | 段落の役割を示す札であって、その文の断定の強さではない。代わりに、札つきの文に言い切りで当たった場合は `needs_human_review` へ |
| 極性の食い違いを落とさず人へ回す | 「ない」がどの述語を打ち消しているかは文単位では分からない。指定文「再生時間だけでは、意味を追えた回数が分かりません」で誤検知が出た |

Threads の1投稿目の下限は120字から100字へ下げました。
指定された本文が108字だったためで、**本文を書き換えないほうを選びました**。

## 例外1件

`THREADS-310-a` の1投稿目が `topic_open`（「〜は、」で始まる）に当たり、
直近10本で3件目の警告が出ています。**指定された本文なので書き換えず、
理由つきの例外**として在庫に記録しました。

## 残っていること

- Threads投稿済み13件の全文（APIは冒頭40字しか返さない）
- 試作の本文はセッション側で書いたもの（API経路は dry run のみ）
- `needs_human_review` は落とさないので、**最後は人の目です**
