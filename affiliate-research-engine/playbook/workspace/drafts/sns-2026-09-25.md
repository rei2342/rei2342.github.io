# SNS投稿ドラフト 2026-09-25（X ×3・note導線）

## 前提
- `playbook/workspace/content-bank/` は空（`.gitkeep` のみ）だったため、
  `CLAUDE.md` の **Track B（英語コーチング・留学、田中さくら）** ペルソナから訴求軸を当てた。
- 文体・語尾ルールは `playbook/config/social/sakura-social-v1.yaml` の `persona` / `style` / `post_types` に準拠。
- **本日のテーマ**: 「英語コーチング・留学サービスは、比べる単位を決めないと選べない」
  （3投稿でテーマの切り口を変え、比較の悩みどころを分担）
- さくら個人の一人称の事実（年齢・TOEICスコア・金額・利用体験など）は `experience_facts.csv` に
  verified な行が無いため**一切使用していない**（★最上位ルール準拠）。
- note記事のURLは未確定のため `[note記事のURL]` をプレースホルダーとして残した。
  投稿前に実URLへ差し替え、`scripts/social_spec.py` のゲート（文字数・語尾・重複等）を必ず通すこと。

## 使った構文（3つ・`post_types` より選定）
1. **misreading（よくある読み違い）** — 投稿1。コーチング＝富裕層向けという思い込みを正す。
2. **checklist（チェックリスト）** — 投稿2。留学エージェント選びで最初に見る項目を渡す。
3. **comparison_axis（比較軸）** — 投稿3。オンライン英会話とコーチングを同じ単位でそろえる。

## NGチェック
- 冒頭挨拶: 3投稿とも話題から書き出しており、挨拶なし。
- 同語尾3連: 各投稿とも文末（です／ます／か／た系）が2連続までに収まるよう確認済み。
- 押し売り感: 「今すぐ」「絶対」等の煽り語・命令形の連発なし。CTAはいずれも提案型。

---

## 投稿1（構文: misreading／CTA: 悩み言語化型）

```
英語コーチングは、お金と時間に余裕がある人向け。そう思われがちです。

実際の分かれ目は、迷った回数を誰かに整理してもらえるかどうか。最初に何を聞くサービスか、そこだけ見ておくと合う・合わないが見えてきます。

「合わなかったらどうしよう」と迷っている人向けに、比較の視点をnoteにまとめました。
[note記事のURL]
```

- 文字数（URL除く・全角換算）: 約145字
- 語尾の並び: です → か → ます → ました（3連続なし）

---

## 投稿2（構文: checklist／CTA: ベネフィット型）

```
留学エージェントは、料金表の安さだけで決めると、あとで比較できなくなります。

まず見たいのは、無料カウンセリングで何を聞かれるか。そこで得意分野が見えてきます。答え方の丁寧さは、メモしておくと後で比べやすいです。

選ぶ前のチェック項目を、noteに整理しました。
[note記事のURL]
```

- 文字数（URL除く・全角換算）: 約129字
- 語尾の並び: ます → か → ます → です → ました（3連続なし）

---

## 投稿3（構文: comparison_axis／CTA: 続き気になる型）

```
オンライン英会話とコーチングは、月額の数字だけそろえると、内容の差が見えにくくなります。

そろえたいのは「話す時間」と「振り返りの有無」の2つ。ここをそろえると、自分に合う形が見えてきます。

実際に比べたら、意外なところで差がついていました。続きはnoteに書いています。
[note記事のURL]
```

- 文字数（URL除く・全角換算）: 約134字
- 語尾の並び: ます → （体言止め）→ ます → ました → ます（3連続なし）

---

## 次の工程（このファイルはまだ在庫化していない）
- note記事側の本文を用意し、実URLを確定させる。
- `scripts/social_spec.py` の該当ゲート（`fact_gate` / `style_gate` / `link_gate` / `duplicate_gate` 等）を通す。
- 通過後に `workspace/social/stock/x/` へ在庫化し、人の承認を経てから投稿する
  （`sakura-social-v1.yaml` の lifecycle: generated → gated → awaiting_approval → approved → posted）。
