# SNS投稿 下書き 2026-09-20（Daily SNS Cannon）

## メタ情報
- フック集: `workspace/content-bank/` は空（`.gitkeep`のみ）→ **CLAUDE.md Track B（英語コーチング・留学／さくらペルソナ）から訴求軸を採用**
- 本日のテーマ: **ワーホリの年齢条件（申請時／渡航時の数え方）**
  - 根拠: `workspace/ga4/LATEST.md`（2026-09-20）で `/working-holiday-age-30/` `/canada-working-holiday-cost/` `/category/海外留学・ワーホリ/` に直近14日の実流入あり。CLAUDE.mdの決済トリガー「年齢条件がある選択肢を逃したくない」に一致
- 選んだ構文（`playbook/config/social/sakura-social-v1.yaml` の `post_types` から3つ）:
  1. **misreading**（よくある読み違い）
  2. **checklist**（チェックリスト）
  3. **direct_answer**（疑問への直接回答）
- 事実台帳（`workspace/experience_facts.csv`）は0行のため、**さくら個人の一人称の過去・年齢・体験は使用していない**。すべて「国ごとの一般的な制度条件」として記述（leak_patternsのexempt扱い）
- 絵文字は `sakura-social-v1.yaml` の唯一の正本ルールに従い、X= 0〜1個・役割語彙（📝☑️💭🌿😌🌸）のみ使用
- ⚠️ `[NOTE_URL]` は未確定のプレースホルダー。**該当テーマのnote記事を公開後、実URL（`?utm_source=x`付き）に置き換えてから投稿すること**
- 状態: `generated`（人の承認前。投稿ツール未接続のためAPI送信はせず本ファイルに保存）

---

## 投稿1（構文: misreading｜CTA: 悩み言語化型）

```
「30歳までに行けば間に合う」と、予定だけ先延ばしにしていませんか。
年齢の数え方は国ごとに違い、申請時点か渡航時点かで条件が変わります。公式サイトでそこだけ確認すると安心です。

「今のままで大丈夫なのか」というモヤモヤを、noteで言葉にしました。
[NOTE_URL]
```
文字数（URL除く・全角換算）: 123字

## 投稿2（構文: checklist｜CTA: ベネフィット型）

```
ワーホリの年齢、「まだ先でいいか」と後回しにしていませんか。
先に見るのは、上限年齢・申請時か渡航時か・発給数に上限があるかの3つです。

国ごとの条件を並べた一覧をnoteに置きました📝。
[NOTE_URL]
```
文字数（URL除く・全角換算）: 91字

## 投稿3（構文: direct_answer｜CTA: 続き気になる型）

```
ワーホリの年齢制限は、誕生日を迎えた瞬間に終わるのでしょうか。
国によって、申請日基準のところと出発日基準のところに分かれます。

主要国を比べたら、思っていたより余裕がある国もありました💭。続きはnoteにまとめています。
[NOTE_URL]
```
文字数（URL除く・全角換算）: 108字

---

## NGチェック
- 冒頭挨拶: なし（3本とも場面・問いから開始）
- 同語尾3連: なし（各投稿の文末は か／ます／です／ました 等で分散、最大連続2）
- 押し売り感: なし（案件名・購入誘導を一切書かず、noteの情報導線のみ）
- 架空の共感（「私も」「わかります」）: 使用なし
- さくら個人の年齢・体験の断定: 使用なし（一般的な制度条件のみ）
- 絵文字: 各投稿0〜1個、役割語彙のみ、3投稿連続の同一絵文字なし
